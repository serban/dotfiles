[user]
  name = Serban Giuroiu
  email = giuroiu@gmail.com

[alias]
  ad = add
  au = add --update
  bl = blame
  bb = branch
  bv = branch --verbose --verbose
  br = branch --verbose --verbose --remotes
  ch = checkout
  cn = clean -d -x --dry-run
  cl = clone
  co = commit
  ca = commit --amend --reuse-message=HEAD
  cb = help  # TODO: Bump
  cp = cherry-pick
  di = diff --stat --unified=5
  ds = diff --stat --unified=5 --staged
  fe = fetch
  gr = grep
  gi = grep --ignore-case
  in = init
  lo = log --decorate --stat
  ls = ls-files
  pl = pull
  ps = push
  pt = push --tags
  rb = rebase
  re = remote
  rs = reset
  sh = show
  st = status
  ta = tag

  sa = stash apply
  sd = stash drop
  sl = stash list
  so = stash show -v
  sp = stash pop
  ss = stash save

[blame]
  showEmail = true

[branch]
  autosetupmerge = true

  # For newer versions of git, this has been superseded by pull.rebase.
  # Keep it for now so that old versions of git still rebase on pull.
  autosetuprebase = always

[core]
  autocrlf = false
  excludesfile = ~/.gitignore
  whitespace = space-before-tab,tab-in-indent,trailing-space

[diff]
  context = 5

[grep]
  lineNumber = true

[merge]
  stat = true

[pull]
  rebase = true

[push]
  default = simple

[rebase]
  stat = true

[color]
  ui = auto

[color "status"]
  header = normal
  branch = normal
  added = green
  changed = yellow
  untracked = red

[color "decorate"]
  branch = green
  remoteBranch = red
  tag = yellow
  stash = yellow
  HEAD = cyan

[color "diff"]
  commit = yellow
  meta = blue
  frag = blue
  func = yellow
  context = normal
  old = red
  new = green

[color "grep"]
  context = normal
  filename = cyan
  linenumber = cyan
  separator = cyan
  function = yellow
  selected = normal
  match = red

[color "interactive"]
  prompt = blue
  header = yellow
  error = red
  help = normal
