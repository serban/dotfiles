#!/usr/bin/env bash

set -o errexit
set -o nounset
set -o pipefail

source "${HOME}/src/dotfiles/bin/osx-preferences.sh"

for plist in ${PLISTS} ; do
  echo "Converting ${plist}"
  binary_to_xml "${plist}"
done
