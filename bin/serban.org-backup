#!/usr/bin/env bash
# vim:set ts=8 sw=2 sts=2 et:

set -o errexit
set -o nounset
set -o pipefail

VHOST_NAME="serban.org"
MYSQL_DATABASE="serban_wp"
NUM_DAYS_TO_KEEP_BACKUPS=30

source "${HOME}/src/dotfiles/bin/wordpress-backup.sh"
